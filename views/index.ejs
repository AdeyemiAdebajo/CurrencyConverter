<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Money Converter</title>
</head>
<body>
  <h1>Money Converter</h1>

  <% if (locals.moneyConverted) { %>
    <p><%= locals.amount %> <%= locals.fromCurrency %> is equivalent to <%= locals.moneyConverted %> <%= locals.toCurrency %></p>
  <% } else { %>
    <p>Enter the amount and select currencies to convert.</p>
  <% } %>

  <form action="/convert" method="POST">
    <label for="amount">Amount:</label>
    <input type="number" id="amount" name="amount" required value="<%= amount || '' %>"><br />

    <label for="fromCurrency">From:</label> 
    <select id="fromCurrency" name="fromCurrency" required>
      <% currencyCodes.forEach(code => { %>
        <option value="<%= code %>" <%= fromCurrency === code ? 'selected' : '' %>>
          <%= code %> – <%= currencyNames[code] %>
          <br />
        </option> 
      <% }) %>
    </select><br />

    <label for="toCurrency">To:</label>
    <select id="toCurrency" name="toCurrency" required>
      <% currencyCodes.forEach(code => { %>
        <option value="<%= code %>" <%= toCurrency === code ? 'selected' : '' %>>
          <%= code %> – <%= currencyNames[code] %>
        </option>
      <% }) %>
    </select>

    <button type="submit">Convert</button>
  </form>
</body>
</html>
